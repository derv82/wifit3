# Environments
.env
.venv/
env/
venv/
ENV/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so

# Tooling
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Project specifics
captures/
data_dumps/
usb_dumps/
scratch/
*.log
# WEP fragmentation/chopchop probe captures — real on-air frames from the
# user's environment; never commit (see no-SSIDs/BSSIDs-in-commits rule).
wifit3-wep-frag*.pcap
wifit3-wep-*.pcap

# If it starts with a dot, committed it gets not
.*
