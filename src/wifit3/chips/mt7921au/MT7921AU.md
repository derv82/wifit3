# MT7921AU Protocol Notes

## Potential Known Gaps

Cross-driver gap classes (project audit 2026-05-25). **Offline analysis only —
no hardware available; verify before `[x]`.**

> **This driver is WIP** — the 802.11 RX path isn't wired: `transport.
> _pkt_drain_loop` (transport.py:282) reads EP 0x84 but only `logger.debug`s
> the bytes; it never dispatches to the subscribed `_on_raw_rx` (driver.py:82).
> So `_on_raw_rx`'s parse→callback (driver.py:174-214) is effectively dead.
> Neither gap is "live" until RX is finished — but build it right:

- [ ] **RX polling loop drops frames** — WILL BE AFFECTED if finished as-is. The
  EP read uses `await loop.run_in_executor(read)` on the loop. When RX dispatch
  is wired, decode/parse/callback should NOT run on the loop — adopt the
  dedicated reader-thread + queue pattern from the start (rtl8821au commit
  2e3a7a7), not the on-loop pattern the older drivers use.
- [ ] **RX filter / monitor mode (ToDS capture)** — DIFFERENT MECHANISM, UNVERIFIED.
  mt7921 is firmware/MCU-driven: monitor is enabled via an MCU "Sniffer Mode"
  UNI command (`_enable_sniffer`, driver.py:117), not register-level RX filters
  like the other mt76 chips. Sniffer mode *should* be fully promiscuous by
  design, but it's unverified — once RX is wired, confirm M2/M4 (client→AP)
  are captured, not just beacons/M1/M3.

This file accumulates **verified** ground-truth facts about the MT7921AU bring-up sequence. Every claim here must be confirmed against `usb_dumps/captures_mt7921u/capture-3.pcap` (the cold-boot Linux trace, bus 4, device 19, vid:pid `0e8d:7961`).

If something isn't in this file, it has NOT been verified — treat anything in `constants.py`, `firmware.py`, etc. as a hypothesis until confirmed here.

## Tooling

- `python scripts/pcap_slicer.py usb_dumps/captures_mt7921u/capture-3_logs/main.log usb_dumps/captures_mt7921u/capture-3.pcap` — maps driver actions to pcap frame ranges. Frames 1–15134 = `<hardware_plugin_and_initialization>` (everything up through firmware load & monitor mode setup).
- For register/macro definitions in the Linux mt76 driver source, `Grep`/`Read` directly against `data_dumps/mt76-source-v6.18/`.

## USB Topology (Interface 3 of MT7921AU)

The MT7921AU exposes a Wireless-class composite device. Interfaces 0/1/2 are standard wireless-class endpoints. **Interface 3 is `Vendor Specific` and is the one the mt76 driver actually uses.**

Endpoints on Interface 3 (from descriptor in capture-3, frames 51–100):

| Address | Direction | Max Packet | Purpose (Linux logical name) |
|---------|-----------|------------|------------------------------|
| `0x84`  | IN  bulk  | 1024       | `MT_EP_IN_PKT_RX` (RX 802.11 packets) |
| `0x85`  | IN  bulk  | 1024       | `MT_EP_IN_CMD_RESP` (MCU command responses) |
| `0x08`  | OUT bulk  | 1024       | `MT_EP_OUT_INBAND_CMD` (MCU commands) |
| `0x04`  | OUT bulk  | 1024       | `MT_EP_OUT_AC_BE` (FW_SCATTER + 802.11 TX data) |
| `0x05`  | OUT bulk  | 1024       | `MT_EP_OUT_AC_BK` |
| `0x06`  | OUT bulk  | 1024       | `MT_EP_OUT_AC_VI` |
| `0x07`  | OUT bulk  | 1024       | `MT_EP_OUT_AC_VO` |
| `0x09`  | OUT bulk  | 1024       | `MT_EP_OUT_HCCA` |
| `0x86`  | IN  bulk  | 2          | unused (event?) |

Linux's `mt76u_set_endpoints` iterates Interface 3's endpoints in descriptor order and assigns logical names in the order `MT_EP_OUT_INBAND_CMD, MT_EP_OUT_AC_BE, MT_EP_OUT_AC_BK, MT_EP_OUT_AC_VI, MT_EP_OUT_AC_VO, MT_EP_OUT_HCCA`. That maps to physical EPs **0x08, 0x04, 0x05, 0x06, 0x07, 0x09**.

**Current `constants.py` has `EP_OUT_FW = 0x08` and `EP_OUT_DATA = 0x09` — both wrong. Correct:**
- `EP_OUT_MCU = 0x08`
- `EP_OUT_FW  = 0x04` (firmware chunks and 802.11 data)

## First Bulk OUT Activity (frames 14182+)

Captured frames after a cold plug-in. Decoded from `usb.capdata`. All multi-byte fields are little-endian.

### Frame layout for an MCU command (non-FW_SCATTER) on EP 0x08

```
[ 4 bytes  ] SDIO header:  tx_bytes(15:0) | pkt_type(17:16)=0
[ 64 bytes ] mt76_connac2_mcu_txd
[ payload  ] command-specific bytes
[ pad      ] to round up total to 4-byte aligned + 4 trailing bytes
```

The 64-byte `mt76_connac2_mcu_txd` is:

```
offset 0  (32 bytes): __le32 txd[8]   -- hardware descriptor
offset 32 (2 bytes):  __le16 len      -- skb->len minus sizeof(txd[8])
offset 34 (2 bytes):  __le16 pq_id    -- 0x8000 = MCU_PQ_ID(MT_TX_PORT_IDX_MCU=1, MT_TX_MCU_PORT_RX_Q0=0x20)
offset 36 (1 byte):   u8 cid          -- command id (e.g. 0x10 = PATCH_SEM_CONTROL)
offset 37 (1 byte):   u8 pkt_type     -- MCU_PKT_ID = 0xA0
offset 38 (1 byte):   u8 set_query    -- MCU_Q_NA=3 for plain commands
offset 39 (1 byte):   u8 seq          -- starts at 1, wraps in low nibble
offset 40 (1 byte):   u8 uc_d2b0_rev  -- 0
offset 41 (1 byte):   u8 ext_cid      -- 0 unless using MCU_EXT_CMD
offset 42 (1 byte):   u8 s2d_index    -- MCU_S2D_H2N = 0
offset 43 (1 byte):   u8 ext_cid_ack  -- 0
offset 44 (20 bytes): u32 rsv[5]      -- 0
```

`txd[0]` and `txd[1]` are populated by `mt76_connac2_mcu_fill_message`:
- `txd[0]` = `MT_TXD0_TX_BYTES(skb->len) | MT_TXD0_PKT_FMT(MT_TX_TYPE_CMD=2) | MT_TXD0_Q_IDX(MT_TX_MCU_PORT_RX_Q0=0x20)`
  → with payload 4: `0x41000044`
- `txd[1]` = `MT_TXD1_LONG_FORMAT | MT_TXD1_HDR_FORMAT(MT_HDR_FORMAT_CMD=1)` = `0x80010000`
- `txd[2..7]` = 0

### Frame 14182: first MCU command sent by Linux

Capdata (76 bytes):
```
44 00 00 00 44 00 00 41 00 00 01 80 00 00 00 00
00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00
00 00 00 00 24 00 00 80 10 a0 03 01 00 00 00 00
00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00
00 00 00 00 01 00 00 00 00 00 00 00
```

Decoded:
- SDIO hdr `0x44` = 68 (txd_len 64 + payload 4)
- `mcu_txd.len` = 0x0024 = 36 = (txd_len_minus_hw_descriptor 32 + payload 4)
- `mcu_txd.pq_id` = 0x8000
- **`mcu_txd.cid` = 0x10 → MCU_CMD_PATCH_SEM_CONTROL**
- `mcu_txd.pkt_type` = 0xA0 (MCU_PKT_ID)
- `mcu_txd.set_query` = 0x03 (MCU_Q_NA — no ext_cid, no CE flag)
- `mcu_txd.seq` = 0x01 (first message)
- Payload (4 bytes): `01 00 00 00` — request semaphore (`PATCH_SEM_GET = 1`)

### Frame 14186: second MCU command (84 bytes, payload 12)

cid = 0x05 = MCU_CMD_PATCH_START_REQ. Payload contains the patch download address/size info.

### Frame 14236: cid=0x07 PATCH_FINISH_REQ
### Frame 14240: cid=0x10 PATCH_SEM_CONTROL (release semaphore)
### Frame 14435+: cid=0x01 TARGET_ADDRESS_LEN_REQ (per RAM section)

### Firmware data chunks on EP 0x04 (frames 14190–14234, etc.)

Each chunk is 4104 bytes:
```
[ 4 bytes ] SDIO header: tx_bytes = 0x1000 = 4096, pkt_type = 0
[ 4096 bytes ] raw firmware payload
[ 4 bytes ] trailing zero padding
```

Linux `__mt76_mcu_send_firmware` paces these out with `cmd = MCU_CMD(FW_SCATTER)`. In `mt76_connac2_mcu_fill_message`, FW_SCATTER short-circuits BEFORE adding the 64-byte mcu_txd — only the 4-byte SDIO header is prepended.

## Findings vs current Python code

The Python implementation in `src/wifit3/chips/mt7921au/` was scaffolded without first decoding the capture. Known incorrect items (do not trust without re-verification):

1. **Endpoints**: `EP_OUT_FW`, `EP_OUT_DATA` numerically wrong (see table above).
2. **`firmware._build_mcu_header`**: builds a 16-byte mash-up that doesn't correspond to either the SDIO header or the connac2 MCU TXD.
3. **`firmware.load_firmware`**: skips the patch-session wrapping (`PATCH_SEM_CONTROL`/`PATCH_START_REQ`/`PATCH_FINISH_REQ`) and the `TARGET_ADDRESS_LEN_REQ` calls needed for the RAM blob.
4. **`firmware.load_firmware` strips first 192/128 bytes of the firmware files** — unverified; Linux uses the full file via `mt76_connac2_fw_trailer` header parsing.

### `DL_MODE` bit position — corrected 2026-05-17

Earlier this doc and `constants.py` claimed the wire `mode` field for plain RAM/patch sections was `0x00800000` (BIT 23), arguing the kernel source's `DL_MODE_NEED_RSP = BIT(31)` was "wrong" because the pcap looked different. That was a **hex-offset miscount** — I lined hex chunks up by eye and ended up 4 bytes short, reading the trailing-pad zeros of `mcu_txd` as if they were the `mode` field. Re-parsed programmatically against capture-3 frame 14186:

```
SDIO hdr (4B) + mcu_txd (64B) + payload (12B struct + 4B pad)
bytes 68-71 = 00 00 90 00  →  addr = 0x00900000   (matches patch base)
bytes 72-75 = 80 67 01 00  →  len  = 0x00016780   (matches patch size 91520)
bytes 76-79 = 00 00 00 80  →  mode = 0x80000000   (= BIT 31, matches source DL_MODE_NEED_RSP)
```

Wire bytes match Linux source. The previous incorrect value was sent on five firmware-load commands (PATCH_START_REQ + 4 × TARGET_ADDRESS_LEN_REQ), causing the boot ROM to accept bytes but never finalize the download — observed symptom was FW_START_REQ jumping into an uninitialized PC and EP0 going dead. Fixed in `constants.py`.

## MCU response behavior on EP 0x85

**Verified 2026-05-17 (with `DL_MODE_NEED_RSP = BIT(31)` correct):**

With the bit set, the device sends a 36-byte `mt76_connac2_mcu_rxd` response on EP 0x85 for every firmware-load command:
- `PATCH_SEM_CONTROL` (GET → `0x02 PATCH_NOT_DL_SEM_SUCCESS`, RELEASE → `0x03 PATCH_REL_SEM_SUCCESS`)
- `PATCH_START_REQ`, `PATCH_FINISH_REQ`
- `TARGET_ADDRESS_LEN_REQ` (one per region)

`FW_START_REQ` does NOT produce a response on EP 0x85 (verified by 0 entries in `_mcu_rx_queue` after sending it). Linux's pcap confirms — no IN data on EP 0x85 anywhere, and zero data captured on EP 0x84 either, until 18ms after FW_START_REQ when a **0-length URB completes successfully on EP `0x84`** as the firmware-boot signal (capture-3 frame 14799 / capture-1 frame 14969).

**MCU command CID routing depends on `MT_WFDMA_HOST_CONFIG_USB_RXEVT_EP4_EN`** (BIT 6 of `0x7c027030`):
- Cleared: MCU responses route to EP 0x85. ← our setup
- Set: MCU responses route to EP 0x84 (verified empirically — PATCH_SEM response moved there when we set the bit).

Linux sets this bit (`mt792xu_dma_rx_evt_ep4`) but appears to tolerate the response-routing change because its `wait=true` calls silently time out (kernel pcap captures zero data on either EP 0x84 or 0x85 IN throughout firmware load). We keep the bit cleared because our code uses the PATCH_SEM_CONTROL response to drive state.

**Current decision:** wait for response on PATCH_SEM_CONTROL; treat data-flow commands as fire-and-forget. The drainer queues other responses silently. Firmware-up signal is `FW_N9_RDY` poll succeeding (which depends on the device's EP0 staying alive — currently the blocker on WinUSB).

## USB 2.0 vs USB 3.0: the original Windows finding (now partially superseded)

**Originally documented as Windows/WinUSB-specific.** On USB 2.0 (HighSpeed) on Windows the firmware upload completes; on USB 3.0 (SuperSpeed) via WinUSB the bulk OUT to EP 0x04 was observed to stall at exactly 4 USB packets. That much is still true on Windows.

**What's NEW (2026-05-19 Kali re-run, see § "Kali re-run 2026-05-19 (post-blacklist)"):** these failure modes are **not WinUSB-specific**. They reproduce on Kali + libusb + USB-3 SuperSpeed once the kernel `mt7921u` driver is properly blacklisted. The Linux-vs-Windows asymmetry is the kernel's USB-stack handling (deep URB pool, kernel completion handlers), not WinUSB itself.

⚠ Don't be fooled by passive "USB 3 to USB 2" adapters — many of them still let the device negotiate SuperSpeed. Use `python scripts/mt7921au/check_usb_speed.py` to verify `speed: 3 (HIGH)`.

## FW_SCATTER 4-packet stall (Windows + Kali on libusb)

The MT7921AU boot ROM accepts exactly **4 USB packets** of FW_SCATTER bulk data, then NAKs subsequent OUTs until the bulk transfer times out. Pattern originally verified on Windows across three chunk sizes:

| chunk_size | wire bytes/chunk | packets/chunk | chunks accepted | total packets |
|-----------:|-----------------:|--------------:|----------------:|--------------:|
| 4096       | 4104             | 4 full + 1 short = 5 (transfer short-writes after 4) | 0 (PyUSB returns 4096/4104) | 4 |
| 1024       | 1032             | 1 full + 1 short = 2 | 2 | 4 |
| 1016       | 1024             | 1 full = 1 | 4 | 4 |

Always 4 packets. `bMaxBurst = 0` in all USB 3.0 SS Endpoint Companion descriptors, so it's not a USB burst-size issue. Most likely: USB 3.0 `NRDY`/`ERDY` flow control between the host USB stack and the boot ROM stack doesn't handshake correctly when the host pipes URBs in one-at-a-time, so after the first batch fills the device's tiny FIFO, the host can't resume.

The kernel-driver pcap (capture-3) shows ~22 4104-byte chunks flowing back-to-back with no stalls, so the **kernel** USB stack handles this fine. That was originally attributed to "Linux vs Windows" but the real distinction looks like "deep URB pool + kernel completion handlers" vs "userspace sync write loop". **On Kali 2026-05-19 run 2 we reproduced the same short-write pattern (4096/4104 on the first chunk) via libusb** — so it's not WinUSB-vs-libusb either.

**Workarounds to investigate:**
1. Force USB 2.0 (plug into USB 2.0 port or hub) — bypasses USB 3.0 flow control entirely. Still the easiest test.
2. Use libusb async URB API (queue multiple URBs) instead of synchronous `Device.write()` — closer to what the Linux kernel does. **Now the strongest hypothesis** (see § "Kali re-run 2026-05-19").
3. Inter-chunk pacing — was tried with `asyncio.sleep`; didn't help on Windows.

## Kali bring-up snapshot (data_dumps/kalitrip, 2026-05-17)

User dumped state from a Kali 2026.1 live USB (`kernel 6.18.12+kali-amd64`) after the same MT7921AU plug-in event we capture on Windows. Highlights:

- **Source-tree version is correct.** Kali loads `mt7921u.ko` from `drivers/net/wireless/mediatek/mt76/mt7921/`. `data_dumps/mt76-source-v6.18/` is the exact source for the binary that bound the device. `Grep`/`Read` citations against that tree are now version-aligned.
- **USB descriptor re-confirmed** against `data_dumps/kalitrip/usb3/lsusb_verbose.txt`. The Interface 3 endpoint table here (EP 0x84/0x85 IN bulk + 0x08/0x04/0x05/0x06/0x07/0x09 OUT bulk + 0x86 IN interrupt @ 2B, all `bMaxBurst = 0`) is byte-for-byte the same as in capture-3, so the EP map above is double-confirmed. The descriptor also shows the device negotiates `SuperSpeed (5 Gbps)` cleanly on Linux.
- **Linux USB 3 SuperSpeed bring-up worked end-to-end.** `dmesg` shows:
  ```
  usb 4-2: new SuperSpeed USB device number 3
  mt7921u 4-2:1.3: HW/SW Version: 0x8a108a10, Build Time: 20250625153620a
  mt7921u 4-2:1.3: WM Firmware Version: ____010000, Build Time: 20250625153703
  ```
  No stall, no retry, no errors. This re-confirms the 4-packet FW_SCATTER stall is **WinUSB/Windows-specific** — the kernel USB stack handshakes USB-3 flow control correctly. Recommended Windows workaround remains: USB 2.0 ports/hubs.

### Firmware variant selection (clarified)

There are two firmware-name pairs in `data_dumps/mt76-source-v6.18/mt792x.h:44-53`:

| SKU | Patch | RAM |
|-----|-------|-----|
| MT7921 (chip_id 0x7961) | `WIFI_MT7961_patch_mcu_1_2_hdr.bin` | `WIFI_RAM_CODE_MT7961_1.bin` |
| MT7920 (promoted from 0x7961 if `MT_HW_BOUND & BIT(7)`) | `WIFI_MT7961_patch_mcu_1a_2_hdr.bin` | `WIFI_RAM_CODE_MT7961_1a.bin` |

The "promote 0x7961 → 0x7920" check lives **only in the PCI driver** (`mt7921/pci.c:357-360`). The USB driver (`mt7921/usb.c:15-30`) unconditionally pins every supported VID:PID to `MT7921_FIRMWARE_WM`, and `modinfo mt7921u.ko` declares only the `_1_` files as firmware deps (`kalitrip/modinfo.txt:6-7`). **Our card (USB `0e8d:7961`) is therefore always the `_1_` variant.** The trailing `a` in `Build Time: 20250625153620a` is just part of the embedded build-stamp inside the patch header — not a variant marker.

### Asset firmware: stale, refreshed 2026-05-17

The blobs previously shipped under `assets/` matched neither Kali's `_1_` nor `_1a_` files (almost certainly an older `linux-firmware` snapshot from when the scaffold was first built):

| File | Old `assets/` sha256 | Kali `_1_` sha256 (now in assets) |
|------|----------------------|------------------------------------|
| `WIFI_MT7961_patch_mcu_1_2_hdr.bin` | `a276c06c…b873f6` | `1cf118a8…f9ce2e` |
| `WIFI_RAM_CODE_MT7961_1.bin`        | `b94217a9…d6a2ab9` | `b42237d2…39d42c6b` |

`assets/` has been replaced with the Kali-shipped `_1_` files (sha256s above, source: `data_dumps/kalitrip/firmware/sha256.txt`). These are committed alongside the code so the driver works on any host with the device attached, no firmware install needed.

This also resolves a previously open hypothesis: failed Windows-USB-2 firmware uploads where the patch session completed but the chip never went to `FW_N9_RDY` may have been masked by wrong RAM-code bytes. Re-test with the refreshed blobs.

## Session pause snapshot (2026-05-17)

Bring-up was paused after extensive iteration in this state:

**Verified correct (don't re-investigate without new evidence):**
- All 6 host-to-device firmware-load MCU commands are **byte-for-byte identical** to pcap (programmatic diff against capture-3 frames 14182/14186/14236/14240/14435/14797). See `_build_mcu_frame` in `transport.py`.
- Endpoints (Interface 3 vendor-specific): IN bulk 0x84/0x85, OUT bulk 0x08 (MCU)/0x04 (FW data + AC_BE)/0x05/0x06/0x07/0x09, IN interrupt 0x86. Confirmed twice — pcap descriptors + `kalitrip/usb3/lsusb_verbose.txt`.
- Firmware blobs in `assets/`: byte-identical to Kali 6.18.12's `_1_` variant (sha256 verified).
- `DL_MODE_NEED_RSP = 0x80000000` (BIT 31) in `constants.py` — matches wire bytes.
- Firmware-load command sequence: PATCH_SEM_GET → PATCH_START_REQ → FW_SCATTER chunks → PATCH_FINISH_REQ → PATCH_SEM_REL → 4× (TARGET_ADDR_LEN + FW_SCATTER chunks) → FW_START_REQ. Matches captures 1 and 3 (capture-2 is a warm boot — chip already running, kernel skipped load).
- Pre-patch DMA scheduler init (`_dmashdl_init` — 16 GROUP_QUOTA + 4 Q_MAP + 2 SCHED_SET + REFILL/PAGE/PKT_MAX_SIZE rmws). Matches `mt792xu_wfdma_init` lines 153-173.
- MCU responses arrive on EP 0x85 with BIT(31) set — see "MCU response behavior" section above.

**Blocker:** on Windows / WinUSB, after `FW_START_REQ` succeeds the chip's EP0 control endpoint goes dead. No data on EP 0x84 (the firmware-up ZLP Linux sees at +18ms), no FW_N9_RDY bit set, no response to any vendor read for 20+ seconds. Same wire bytes as Linux's pcap up through FW_START_REQ produce different post-boot behavior.

**Hypotheses for next session (not yet tested):**
1. WinUSB on Windows mishandles the USB-stack event the device emits at firmware handoff (ZLP, soft reset, port reconfig). Kernel handles it transparently, WinUSB doesn't surface it.
2. The device requires a larger pool of pre-submitted bulk-IN URBs than our one-at-a-time `dev.read()` synchronous loop maintains. Linux pre-submits 128 per endpoint via `mt76u_alloc_queues`.
3. Some pre-patch init we haven't replicated — but DMASHDL + WLCFG + dma_rx_evt_ep4 cover the known cold-boot register writes. `mt792xu_dma_prefetch` (TX ring extension control, 7 RMWs) is the only remaining unimplemented chunk of `mt792xu_wfdma_init`, likely not boot-critical.

**Recommended first move on resumption:** test the current code on Kali (boot live USB, `rmmod mt7921u`, install Wifit3, run `python scripts/mt7921au/test_hw_mt7921au.py`). If firmware boots on Kali but not on Windows, hypothesis 1 or 2 is confirmed and we focus on Windows USB compat. If it fails on Kali too, we have a genuine code bug we haven't isolated yet — and Kali gives us full `dmesg` / `usbmon` for debugging.

**Known Windows-specific compat issues (don't re-discover):**
- UHW bus (`bmRequestType 0x5E/0xDE`) returns `Errno 5 I/O Error` on every transfer. Not used for cold-boot anymore (bulk-EP reset bits are already clear — `_epctl_release_reset` is a no-op state change).
- USB 3.0 SuperSpeed has the 4-packet FW_SCATTER stall (see section above). USB 2.0 ports/hubs work.

## Kali re-run 2026-05-19 (post-blacklist) — the big diagnostic update

User re-ran `scripts/kali_test_mt7921au.sh` (which installs the
`mt7921u`/`mt7921_common` modprobe blacklist, `rmmod`s the mt76 family,
prompts for replug, then runs the test) twice on the same Kali 2026.1
live USB. Bundles in `usb_dumps/wifit3-kali-bundle/run-20260519T191052Z/`
(fresh device, first plug-in of the session) and
`.../run-20260519T191446Z/` (replug-after-wedged-run).

**Headline:** the kernel-driver-collision theory from
[KALI-HANDOFF-2026-05-19.md](./KALI-HANDOFF-2026-05-19.md) was right —
but clearing it exposed a **new, deeper blocker that ALSO reproduces on
Kali**. The "WinUSB-specific" hypothesis is dead.

### Run 1: cleanly blacklisted, fresh enumeration

`dmesg_during.txt` shows no `mt7921u` lines anywhere during the test;
`device_sysfs_post_replug.txt` and `device_sysfs_post_test.txt` both show
`4-2:1.3 -> (none)`. Clean baseline.

Then test_stderr.txt:

| Step | Result |
|------|--------|
| Claim interface 3 | ✅ |
| MT7921 detected: chip_id=0x7961 | ✅ |
| MCU power-on (poll `0x7c0600f0` BIT0) | ✅ in 11 ms |
| `_dma_init` (RMWs at `0x7c026010` / `0x7c02600c` / `0x7c02601c` / `0x54000120` / `0x74000018` / `0x7400000c`) | ✅ |
| PATCH_SEM_GET (cid=0x10 seq=0x01, EP 0x08) | ✅ — **this was the prior Kali failure point** |
| PATCH_SEM_GET response on EP 0x85 | ✅ — 36-byte rxd, status `0x02 PATCH_NOT_DL_SEM_SUCCESS` |
| PATCH_START_REQ + 23 FW_SCATTER chunks (92,032 B patch) | ✅ |
| PATCH_FINISH_REQ + PATCH_SEM_REL | ✅ |
| 4× (TARGET_ADDR_LEN_REQ + FW_SCATTER chunks) for WM regions 0/1/2/3 | ✅ — 89 + 67 + 4 + 13 chunks, ~700 KB total |
| WM region 4 (NON_DL, addr=0x00000000 len=88416 feature_set=0x40) | ✅ skipped per source |
| FW_START_REQ bulk OUT (cid=0x02 seq=0x09, EP 0x08) | ❌ **2-s timeout** (`[Errno 110] Operation timed out`) |
| Subsequent vendor reads on EP0 (`0xdf 0x63` and `0xc0 0x01`) | ❌ all timed out for ~25 s |
| `dmesg_during.txt` after test | `usb 4-2: Failed to suspend device, error -110` — chip is wedged at the USB-stack level, not just our state machine |

This is the **same Windows wall**, give or take a packet: on Windows the
FW_START_REQ bulk OUT was observed to complete and EP0 went dead
immediately after; on Kali the FW_START_REQ bulk OUT itself times out.
Either way the chip stops accepting traffic around firmware handoff.

### Run 2: dirty device, less interesting

dmesg shows the chip was wedged from Run 1 (two `Failed to suspend
device, error -110` lines before user replugged). User replugged ~4 s
before re-running (Run 1 had a clean ~10 s gap), and:

| Step | Result |
|------|--------|
| Up through PATCH_START_REQ | ✅ |
| FW_SCATTER patch chunk 0 (4096 B request) | ❌ **short bulk write 4096/4104 bytes** after 5 s |

That's the canonical Windows "4-packet stall" pattern reproduced on Kali
+ libusb. So either the device was still half-dead from Run 1 OR the
4-second post-replug settle was too short for a clean cold-boot OR the
4-packet stall genuinely affects Linux + libusb-sync the same way it
affects WinUSB-sync.

### What this rules out / in

**Dead** (after this run):
- "WinUSB mishandles the firmware-handoff USB event" — same wall on
  Linux + libusb.
- "WinUSB-specific NRDY/ERDY flow-control bug" for the 4-packet stall
  — reproduced on Linux + libusb in Run 2.

**Strongest surviving hypothesis:**
- **Shallow bulk-IN URB pool.** Linux's kernel driver pre-submits 128
  URBs per IN endpoint via `mt76u_alloc_queues` before any firmware
  traffic flies. Our transport submits one bulk-IN read at a time on
  the drainer thread. Around FW_START_REQ the device emits both a
  status response on EP 0x85 AND a 0-length URB completion on EP 0x84
  (the "fw_up" signal Linux sees at +18 ms); if we're not in a posted
  state to receive both, the boot ROM's flow-control may interpret the
  silence as host-not-ready and wedge.

  This also explains the 4-packet stall — the boot ROM may be using
  flow control across both directions simultaneously. With only one
  bulk-IN URB posted, after a few OUTs the device wants to send an
  internal ACK/event and can't, so it stops accepting OUTs.

  Fix would be: rewrite `transport.py` to use the libusb async URB API
  (`libusb_submit_transfer`) and pre-submit ~32+ URBs on EP 0x84 and EP
  0x85 before firmware load begins. PyUSB has thin wrappers in
  `usb.core.Endpoint.read()` but the async path is more naturally done
  via the underlying `libusb1` binding.

**Not yet ruled out:**
- Some pre-patch register write we haven't replicated (only
  `mt792xu_dma_prefetch` is the remaining unimplemented chunk of
  `mt792xu_wfdma_init`; not believed boot-critical, but unverified).

### Bundle gotcha for next time

`sudo tshark -i usbmon0 -w "$OUT_DIR/usbmon0.pcap"` hit `Permission
denied` writing into the bundle dir (`tshark.log` line 3) on both runs.
Standard Kali setup: tshark drops to the `wireshark` group, can't write
to a `kali:kali` path. So **both bundles are missing the pcap**. Logs
alone were enough to call the failure, but for the next round the
script should either (a) `sudo chown $USER:wireshark $OUT_DIR &&
chmod 775 $OUT_DIR` before launching tshark, or (b) write to `/tmp`
then move. Worth fixing in `scripts/kali_test_mt7921au.sh` before the
next bring-up attempt.

### Companion docs

- Bundles: `usb_dumps/wifit3-kali-bundle/run-20260519T191052Z/` (Run 1
  — got furthest), `.../run-20260519T191446Z/` (Run 2 — short-write).
- Original handoff context: [KALI-HANDOFF-2026-05-19.md](./KALI-HANDOFF-2026-05-19.md).

## Kali handoff (2026-05-19) — see [KALI-HANDOFF-2026-05-19.md](./KALI-HANDOFF-2026-05-19.md)

User ran `scripts/mt7921au/test_hw_mt7921au.py` on Kali 2026.1 live USB against the
same code as the 2026-05-17 pause snapshot. **Test failed earlier than on
Windows** — the very first MCU command (`PATCH_SEM_GET`, cid=0x10, seq=0x01,
EP 0x08) errored out at the bulk OUT, never reaching response wait. This
is the same byte-for-byte command verified against capture-3 frame 14182.

Most-likely culprit: **kernel `mt7921u` driver was still bound** to the device's
Interface 3 when the test ran. dmesg shows `mt7921u` repeatedly initializing on
this device through the session (vendor-request `-110 ETIMEDOUT` bursts every
~3.2 s), and the device migrated Bus 4 → Bus 2 mid-session. `lsusb -t` showing
`Driver=[none]` on If 3 may have been a stale snapshot or the brief window before
the kernel re-grabbed it.

Companion raw evidence (lsusb, dmesg, usbmon2 capture, test output) is in
`usb_dumps/notes_mt7921au/`.

**Next-resumption recipe** (lifted from the handoff doc):
1. `echo 'blacklist mt7921u' | sudo tee /etc/modprobe.d/wifit3.conf` (+ `mt7921_common`)
2. `sudo rmmod mt7921u mt7921_common mt76_connac_lib mt76_usb mt76`
3. Replug, verify `lsusb -t` + `dmesg | tail` show no `mt7921u` lines
4. Re-run test. Capture fresh `usbmon` on the actual bus the device enumerates on.
5. If `PATCH_SEM_GET` still fails after a clean blacklist+replug → isolated code bug, not a Windows-only blocker. If it succeeds (or fails later) → driver-collision theory was the right diagnosis and we go back to the Windows hypotheses (#1 WinUSB mishandles FW handoff event; #2 shallow bulk-IN URB pool).

The handoff doc has the full TL;DR, dmesg timeline, lsusb topology, the
usbmon analysis recipe, and what's still verified from the 2026-05-17 pause
snapshot. Read it first when resuming MT7921AU work.

## Windows USB 2.0 re-baseline 2026-05-20

After the mt76x2u driver landed (MT7612U feature-complete, hw-verified on
the same dev machine), user re-ran `test_hw_mt7921au.py` on Windows to
re-establish the current MT7921AU state. Three USB configurations tested
via `scripts/mt7921au/check_usb_speed.py`:

| Path | Negotiated | maxPacketSize | Result |
|---|---|---|---|
| USB-C native | SuperSpeed 0x0320 (USB 3.0, 5 Gbps) | 1024 | **EARLIEST** failure — no PATCH_SEM_GET response on EP 0x85 (chip not visible to bulk-IN; possibly USB-C link-state handling artifact). |
| USB-C → USB-A adapter | SuperSpeed 0x0320 (still 5 Gbps!) | 1024 | 4-packet stall at first FW_SCATTER chunk (`Short bulk write 4096/4104`). Passive USB-A adapters do NOT force HighSpeed — they let SuperSpeed negotiate. |
| USB-C → adapter → "the 32X" extender | **HighSpeed 0x0210 (USB 2.0, 480 Mbps)** | **512** | **FW upload completes through.** Wall is at FW_START_REQ. ← matches 2026-05-17 baseline + Kali Run 1. |

The 32X is a separately-purchased passive USB extender that hard-downgrades
to USB 2.0. The full stack is **USB-C → USB-A adapter → 32X extender →
MT7921AU**. Flaky (jiggle the cable and it drops), but it's the only
Windows path that lands on USB 2.0 / HighSpeed currently available on this
dev machine.

### The wall — confirmed cross-platform 2026-05-20

USB 2.0 path on Windows today reproduces the 2026-05-17 documented
behaviour *and* matches the 2026-05-19 Kali Run 1 result exactly:

| Step | Today (Windows USB 2.0) | Kali Run 1 (2026-05-19) | 2026-05-17 (Windows USB 2.0) |
|---|---|---|---|
| PATCH phase | ✅ | ✅ | ✅ |
| 4× WM regions | ✅ | ✅ | ✅ |
| FW_START_REQ bulk OUT | ❌ `Errno 10060` (timeout, 2 s) | ❌ `Errno 110` (timeout, 2 s) | (succeeded historically) |
| Subsequent EP0 vendor reads | ❌ all timeout `Errno 10060` | ❌ all timeout `Errno 110` | ❌ all timeout |

`Errno 10060` (Windows `WSAETIMEDOUT`) and `Errno 110` (Linux `-ETIMEDOUT`)
are the same error. Today's Windows run shows the FW_START_REQ bulk-OUT
itself timing out — which is slightly EARLIER than the 2026-05-17 baseline
recorded (where the OUT succeeded and EP0 died after). The wall has
shifted by ~2 ms but it's the same wall.

**Headline:** *the FW_START_REQ wall is universal*. Not WinUSB-specific.
Not Linux-libusb-specific. Same wire bytes, same endpoint, same error,
same EP0-death aftermath on both platforms.

### What we just gained

- **Windows testing is now viable for the actual blocker.** Previously the
  understanding was "USB 3.0 SuperSpeed has the 4-packet stall; need Kali
  to test the FW_START_REQ wall properly." That's now wrong. The 32X
  stack gets us to the same FW_START_REQ wall on Windows. Diagnostic +
  fix-attempt iteration no longer needs a Kali round-trip.
- **The two MCU response messages drained AFTER FW_START_REQ's failed OUT**
  (timestamps in run output) are late acks for the last TARGET_ADDR_LEN_REQ
  commands — meaning the chip is still happily processing requests at the
  moment we send FW_START_REQ. The chip is NOT wedged before the OUT; it
  wedges *during* the OUT.

### Strongest hypothesis remains: shallow bulk-IN URB pool

The single mechanism that differs between us and the kernel across both
platforms is the URB pool depth on EP 0x84 + EP 0x85:

- Kernel: `mt76u_alloc_queues` pre-submits 128 URBs per IN endpoint.
- Us: one bulk-IN read at a time on each drainer thread.

At FW_START_REQ time, the chip may emit an internal event or expect an
ACK URB to be receivable. If we're between drainer iterations (gap
between completion and re-post) when the chip looks for that posted URB,
USB-3 flow control causes the OUT to NAK-loop until 2 s timeout. On
USB-2 the same gap may matter less for the *4-packet stall* (which we no
longer see), but evidently still matters for the *FW_START_REQ* case.

Fix path: restructure `chips/mt7921au/transport.py` to use libusb async
URB API (`libusb_submit_transfer`), pre-submit ~32 URBs each on EP 0x84
and EP 0x85 before FW upload. Testable directly on Windows now via the
32X stack.

## Not yet verified (do not implement until added here)

- Sniffer mode enable command bytes.
- Channel switch command bytes.
- RXD/TXD layout for actual 802.11 frames (not firmware data).
