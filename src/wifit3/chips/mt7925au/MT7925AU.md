# MT7925AU

MediaTek MT7925U (Wi-Fi 7, connac3) USB driver port.

## Captured Wireless Card

- MediaTek MT7925U, dual-band (2.4 + 5 GHz), Wi-Fi 7 / connac3, USB.
- Captured unit: Netgear A9000, `VID:PID 0846:9072` (the driver also claims MediaTek
  `0e8d:7925`). USB SuperSpeed.
- Captures: `usb_dumps_new2/captures_mt7925u/` (capture-1/2/3 + per-run logs +
  `firmware/`). Recorded on Kali, kernel `6.19.14+kali-amd64`, driver `mt7925u`.

## Linux Driver Source

- Upstream: `git.kernel.org` linux-stable, tag `v6.19.14`, commit
  `b9dbb4576bc8c69e87b4ca4caa2fb1c0db47d722`; subtree
  `drivers/net/wireless/mediatek/mt76/` (mt7925-specific in `mt7925/`).
- Type: mainline. Vendored (gitignored) at `data_dumps/mt76-source-v6.19.14/` with a
  `VERSION.md` provenance marker.
- The mt792x/connac register layer is shared with mt7921; the chip-id, firmware
  container and connac3 MCU/descriptor formats are mt7925-specific.

## Python Port Details

- `VID:PID 0846:9072` + `0e8d:7925`, selected by `discovery.py` (`mt7925au`).
- Status: the full cold-boot bring-up is pcap-verified end to end (all 3 captures,
  **zero divergence, every driver op reproduced, 99.7% = 7082/7104 ops**): firmware
  upload, post-boot device init (`init.py`), the operational `mac_work` survey/MIB reads
  (`mac.py`), monitor entry, the channel sweep (`config_sniffer`, ch 1-165), the per-band
  TX power tables (`txpower.py`, 58 `SET_POWER_LIMIT`), and the world-"00" channel domain
  (`mcu.set_channel_domain`, 2 `SET_DOMAIN_INFO`). The only 22 waived ops are OS USB
  enumeration (usbcore GET_DESCRIPTOR / SET_CONFIGURATION), which no wifi driver emits.
- **RX works on hardware and matches a known-good card**: cold boot + monitor entry
  succeed on the Netgear 0846:9072, GET_NIC_CAPAB reads the MAC. `rx.decode_frame`
  (connac3 RXD) is ported. beacon_watch A/B vs a known-good ALFA AWUS036ACHM (MT7610U),
  same AP and moment: 2.4 GHz ch1 = 155 beacons (mean 7.8/s) vs the ALFA's 154 (7.7/s);
  5 GHz ch161 = 191 (mean 9.6/s) vs the ALFA's 195 (9.8/s, ~98%). On par on both bands.
- **TX (inject) works on hardware**: `tx.build_tx` (connac3 TXWI, `mt7925_mac_write_txwi`)
  reproduces every injected mgmt frame in both TX captures byte-exact (573/573 on each:
  deauth + disassoc + probe req; TX BYTE-MATCH phase in `scripts/mt7925au/verify_pcap.py`).
  Live injection confirmed end to end in wifit3 on 2.4 + 5 GHz: deauth drops clients and
  the 4-way / PMKID handshakes are captured on reconnect.
- Warm-reattach detects a warm chip but only asks for a replug (no DMA-resume).
- Related: `mt7921au` (connac2 sibling; same mt76-USB transport, different MCU/txd).

Non-obvious in the port:

- **TX TXWI context is the monitor vif's, not defaults.** `mt7925_mac_write_txwi`'s
  per-frame fields come from the monitor link (mt7925/main.c:375-392): band_idx = `0xff`
  which `FIELD_PREP(MT_TXD1_TGID=GENMASK(13,12))` masks to **3** (TGID=3 on every frame),
  wcid = `MT792x_WTBL_RESERVED` (19), and `basic_rates_idx = MT792x_BASIC_RATES_TBL + 4`
  = **15** (TX_RATE) because the phy's default chandef lands on 5 GHz (the last band with
  world-enabled channels, mac80211.c:395-418), so main.c:382 takes the non-2.4GHz branch.
  These are constants for the monitor vif; deriving them from the inject channel is wrong.
- **connac3 txd[1] = `0x00004000`** (HDR_FORMAT_CMD at GENMASK(15,14)), not connac2's
  `0x80010000`. The single most likely mis-port when adapting from mt7921 (mcu.py).
- **MCU response header is `mt7925_mcu_rxd` (44 B)**: `rxd[8]` then len/pkt_type_id,
  so eid@36, seq@37 (connac2 was 28 B, seq@29). Used for MCU seq matching.
- **INIT_DOWNLOAD command is addr-driven**: `PATCH_START_REQ` iff addr is `0x900000`
  or `0xe0002800` (mt7925 adds the second), else `TARGET_ADDRESS_LEN_REQ`.
- **The ROM patch is AES-encrypted** (both sections): the DL_MODE comes from the
  section's `info.sec_key_idx` word (offset 20, BE) via `mcu.get_data_mode`
  (`0x80000009` for the reference patch), not a plain `NEED_RSP`.
- **epctl over the UHW bus**: `mt792xu_epctl_rst_opt` uses bRequest 0x01/0x02 on the
  Linux wire (byte-exact, no waiver). On WinUSB the UHW bus returns Errno 5, so
  `firmware._epctl_rst_opt` falls back to the unified bus.

## Known Problems

- **TX ctrl frames (RTS) not reproduced.** The TX captures carry 90 aireplay-injected RTS
  frames whose TXWI tid comes from `skb->priority` (mt7925_mac_write_txwi_80211), absent
  from the frame and underivable offline. wifit3 injects only the mgmt frame it is given,
  never RTS, so these stay out of scope (TX-capture only, not in the RX/monitor captures).
- **Warm reattach is a stub.** `_warm_reattach` re-posts RX + re-reads the MAC, but the
  warm chip's bulk pipes wedge on WinUSB; it asks for a replug. A full DMA-resume
  reattach (mt7921u_resume: `mt792x_dma_need_reinit` + `dma_init(resume)`) is a TODO.

## Driver Entry Points

- Firmware download: `firmware.load_firmware` (`mt7925_run_firmware` ->
  `mt792x_load_firmware` -> connac2 patch/RAM/FW_START); DMA in `firmware._dma_init`.
- Post-boot init: `init.post_boot_init` (run_firmware tail, set_eeprom, `_mac_init`).
- Monitor + channel: `mcu.uni_dev_info/uni_bss_info/set_sniffer/config_sniffer/
  set_rxfilter`; `driver.set_channel` (config_sniffer).
- Periodic reads: `mac.update_survey/update_mib_stats/reset_counters`
  (`mt792x_mac_work`).
- TX power tables: `txpower.rate_txpower_all` (`mt7925_mcu_rate_txpower_band` +
  `build_sku`); RX decode: `rx.decode_frame` (`mt7925_mac_fill_rx`).
- TX inject: `tx.build_tx` (`mt7925_mac_write_txwi` + `_write_txwi_80211`, USB SDIO
  layout from `mt7925_usb_sdio_tx_prepare_skb`); `driver._inject_frame` sends it on EP
  0x09 (HCCA); `driver._stamp_tx_seq` assigns the live 802.11 sequence.
- MCU framing: `mcu.build_mcu_frame` (`mt7925_mcu_fill_message`; 64B mcu_txd / 48B
  uni_txd, txd[1]=0x00004000).
- Register access: `transport.py` (unified bus 0x63/0x66, UHW bus 0x01/0x02).

## Scripts

- `scripts/mt7925au/verify_pcap.py`: single-cursor pcap verify (firmware +
  post_boot_init + operational dispatch), on the shared `scripts/mt76_verify_replay.py`
  engine, plus a TX BYTE-MATCH phase (auto-runs when the capture carries EP-0x09 TX
  frames). Run via `uv run python scripts/verify_pcap.py mt7925au <pcap>`.

## Handoff: remaining work (fresh session)

Verify with `uv run python scripts/verify_pcap.py mt7925au usb_dumps_new2/captures_mt7925u/capture-1.pcap`
(99.7% clean; run all 3 caps) and the TX captures (`captures_mt7925u_tx/capture-1/2`,
TX BYTE-MATCH = 573/573). Ordered by value:

1. **TX inject (M5): DONE, works on hardware.** `tx.build_tx` (connac3 TXWI) reproduces
   every injected mgmt frame in both TX captures byte-exact (573/573: deauth + disassoc +
   probe req); `driver._inject_frame` (EP 0x09 HCCA) + `_stamp_tx_seq` wired. Live
   injection confirmed in wifit3 on 2.4 + 5 GHz (handshakes/PMKID captured after deauth).
   `verify_pcap.py` grew a TX BYTE-MATCH phase (the operational tail interleaves mac_work
   reads with TX on separate threads, not strict-cursor-walkable, so it diffs TX frames
   directly, methodology 6.4). 90 aireplay RTS frames are out of scope (tid from
   skb->priority). See tests/chips/mt7925au/test_tx.py.
2. **RX quality A/B: DONE, on par with a known-good card.** beacon_watch, same AP/moment,
   vs an ALFA AWUS036ACHM (MT7610U): 2.4 GHz 155 vs 154 beacons; 5 GHz 191 vs 195 (~98%).
   Both bands match the reference (see Python Port Details).
3. **`SET_DOMAIN_INFO` (2 ops): DONE.** `mcu.set_channel_domain` ports the world-"00"
   regdom per-channel flags (byte-identical to Linux region-00), reproduced byte-exact,
   and `init.enter_monitor` now sends it. Verify is down to 22 waived = OS USB enumeration
   only. Reaching a true 100% would require reproducing usbcore's enumeration, which no
   wifi driver emits.
4. **Warm-reattach DMA-resume** (see Known Problems) so reconnect needs no replug. No
   suspend/resume capture exists to byte-verify against; needs hardware iteration.
5. **DONE: `scripts/mt7925au/test_hw.py`** (RX validation + a user-gated `--tx` deauth
   burst), **CREDITS.md** (mt76 team mapped to MT7925AU: connac3 reuses the mt76/mt792x/
   connac stack the MT7921AU contributors built; mt7925-specific = Deren Wu + Lorenzo
   Bianconi), **FIRMWARE.md** (the 2 blobs from `linux-firmware` `mediatek/mt7925/`, FW
   download byte-exact in verify_pcap).

Scratch analysis scripts used this session live in the session scratchpad (dump_tx,
decode_tx, dump_domain): not committed; re-derive as needed.

## Debug log

- 2026-07-26: RX quality A/B passed. beacon_watch (same AP, same moment) vs a known-good
  ALFA AWUS036ACHM (MT7610U): 2.4 GHz ch1 155 vs 154 beacons (mean 7.8 vs 7.7/s), 5 GHz
  ch161 191 vs 195 (mean 9.6 vs 9.8/s, ~98%). MT7925AU RX is on par with the reference on
  both bands, no beacon-loss cliff. Clears methodology Step 4.
- 2026-07-26: SET_DOMAIN_INFO ported (`mcu.set_channel_domain`, world-"00" regdom). The
  per-channel flags are cfg80211's `ieee80211_channel_flags` for region "00" applied to
  the mt76 channel plan (14x 2.4 GHz + 25x 5 GHz; 6 GHz fully world-disabled). Encoded as
  a documented table (constants.CHAN_* + mcu.W00_2G/W00_5G), byte-identical to the capture
  and to any Linux region-00 box, rather than porting cfg80211's regulatory engine + the
  un-vendored reg.c world_regdom. World "00": NO_IR on 2.4 ch12-14 + all 5 GHz, DSSS-only
  ch14, DFS on 5 GHz 52-64 + 100-144, 2.4 GHz capped at 40 MHz, HT40+/- cleared at sub-band
  edges, NO_320MHZ everywhere. `init.enter_monitor` now sends it (after deep-sleep, before
  the txpower batch, per the capture). Verify: 7082/7104 = every driver op reproduced; the
  22 remaining waivers are OS USB enumeration. HW re-validated (RX 2.4 + 5 GHz clean).
- 2026-07-26: TX (M5) ported + byte-verified. `tx.build_tx` reproduces all 573 injected
  mgmt frames per TX capture byte-exact (both caps). Field derivation confirmed against
  the wire: txwi[0] TX_BYTES=mpdu+64 / PKT_FMT=SF / Q_IDX=ALTX0; txwi[1] FIXED_RATE |
  HDR_INFO=hdrlen/2 | HDR_FORMAT=802_11 | TGID=3 (band_idx 0xff masked) | WLAN_IDX=19;
  txwi[3] SN_VALID | BA_DISABLE | REM_TX=15 | NO_ACK | SEQ from the frame's seq_ctrl;
  txwi[6] DAS | MSDU_CNT=1 | DIS_MAT | TX_RATE=15 (basic_rates_idx, 5 GHz default chandef).
  USB layout [4B SDIO hdr][64B TXD][MPDU][pad], pad=round_up(4+64+mpdu,4)-that+4. The 90
  RTS frames per cap differ only in TID (skb->priority=7), not injected by wifit3.
- 2026-07-26: the 60 "regulatory" waived ops are `mt7925_mcu_rate_txpower_band`
  (SET_POWER_LIMIT tag=0x1, per-band TX power, USB-reachable) + one
  `mt7925_mcu_set_channel_domain` (SET_DOMAIN_INFO). NOT the CLC path (tag=0x3,
  USB-disabled). Confirmed on the Kali box: the in-tree v6.19.14 mt76 source is
  byte-identical to the build that produced the pcap (no Kali patch). So they port
  from source, not by snapshot. Bodies = per-rate SKU from world-"00" 20 dBm.
- 2026-07-26: txpower ported (`txpower.py`); the 58 SET_POWER_LIMIT reproduce
  byte-exact. Per-channel power is binary: enabled 20 MHz channels 0x28 (20 dBm),
  world-disabled channels (5 GHz intermediates + all 6 GHz) a whole-0x7f SKU (get_ch_power
  returns the unset tx_power=127). build_sku byte order = cck/ofdm(stride40)/ht(8+8+1)/
  vht(4x10 stride12)/he(7x12)/eht(16x16) into 449 bytes.
- 2026-07-26: full cold-boot bring-up byte-verified against all 3 captures (99.7%,
  7080/7104 ops, 0 divergence). RX HW-validated (710 frames/32 BSSIDs, 2.4 GHz).
  connac3 deltas vs mt7921: txd[1]=0x00004000, mt7925_mcu_rxd seq@37, 48B uni_txd,
  CHIP_CONFIG/HIF_CTRL clear the option ACK bit, addr-driven INIT_DOWNLOAD
  (+0xe0002800), AES patch sections (mode from info word @20), UHW-bus epctl, MDP base
  0x820cc800, MT_WTBL_UPDATE 0x820d4380 GENMASK(11,0)/BIT(14), rxfilter via UNI
  BAND_CONFIG (mode fif?0:1). No TX in the captures (aireplay injection failed).
