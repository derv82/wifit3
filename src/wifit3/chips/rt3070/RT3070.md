# RT3070 (ALFA AWUS036NH) — clean-room port brief

Self-contained handoff for an autonomous port. Read this top-to-bottom, then
`planning/PORTING.md` (the playbook), then start at M1. Everything below the "Verified
facts" header was decoded off **this card's own cold-boot capture** + the kernel source —
not assumed.

---

## ⛔ Clean-room rules (read first — these are load-bearing)

1. **Port from the kernel C, not from a sibling.** The source of truth is
   `data_dumps/rt2x00-source-v6.18/{rt2800usb.c,rt2800lib.c,rt2x00usb.c}` + this card's
   pcap. **Do NOT copy logic from `chips/rt2800usb/`.** Treat it as a *structural* reference
   only — module layout, the `WlanDriver` Protocol surface — never a logic source.
2. **The shared base is proven NOT byte-perfect.** `chips/rt2800usb/eeprom.py` has a
   **confirmed EFUSE addressing bug** (byte offset where the chip wants a u16-word offset —
   details below). It shipped because the old gate only checked firmware upload. If you copy
   it, you inherit the bug. This is exactly the "oh, that's how it's done" trap — don't fall
   in. When in doubt, the **kernel C is right and the wire is the test.**
3. **The wire is your unit test AND your compass.** `verify_pcap.py rt3070` is one monotonic,
   fail-closed walk; the first unaccounted op *is* the next thing to port. **Never edit the
   recipe to make it print PASS** (a prior rt2800usb gate was gutted to do exactly that).
4. **Port everything; skip nothing.** The #1 bug class on this project is partial ports —
   "we don't need X for monitor+inject" drops crucial RX/TX-power / RF / calibration / AGC
   logic. If a path can't be verified on *this* 1T1R hardware but another card would exercise
   it, port it faithfully and mark `#TODO untestable: <why>` — don't omit it.
5. **No live TX. Ever.** You may run bring-up scripts on the live (Zadig-bound) card and
   `verify_pcap`, but **frame injection / deauth is a human's job** [[passive_by_default]].
   You *wire* TX (descriptor build, bulk-OUT path) and pcap-verify it; you do not fire it.

---

## TL;DR — the decisions (lead-approved)

- **Standalone `chips/rt3070/`** — full clean-room driver, 100% anti-DRY. Become the *first*
  byte-perfect member of the rt2x00 family; a shared base gets extracted *later*, after a
  second member is byte-perfect too. Delete the current placeholder `driver.py` as you go.
- **Claim `148f:3070` only** in `SUPPORTED_IDS`. RT3071/RT3072/RT3090 take materially
  different register branches (not "just antennas") that we cannot test — so port those
  branches faithfully **with `#TODO untestable`**, but don't *advertise* a PID we've never
  run a byte against. Enabling them later is a one-line `DeviceID` + clearing the TODO.
- **Own, correct EFUSE reader** (u16-word offset). Do not touch the buggy shared one; it's
  filed for the future family-perfection pass (`chips/rt2800usb/RT2800USB.md` known-gaps).

---

## Verified facts (decoded from `usb_dumps_new/captures_rt3070/capture-1.pcap`)

| Fact | Value | Source |
|------|-------|--------|
| USB ID | `148f:3070` | lsusb / capture |
| Silicon / rev | `0x3070` / `0x0201` = **`REV_RT3070F`** | `MAC_CSR0` read @ frame 482; `rt2800.h:82` |
| RF chip | **RF3020** (RF_TYPE field = 5) | EFUSE `NIC_CONF0=0x0511`; `rt2x00.h` RF enum |
| Chains | **1T1R** (txpath=1, rxpath=1) | EFUSE `NIC_CONF0=0x0511` |
| EEPROM kind | **EFUSE** (97 `EFUSE_CTRL` touches; zero `USB_EEPROM_READ`) | capture |
| EFUSE state | **burned/healthy** (8/32 blocks programmed; real MAC; real freq trim) | reconstructed |
| MAC | `00:c0:ca:59:cd:9f` | EFUSE words 0x02–0x04 |
| freq_offset | **10** (`FREQ` word 0x1D low byte = 0x0a) | EFUSE |
| NIC_CONF1 | `0x00a6` — **external 2.4 GHz LNA present** (bit2 `EXTERNAL_LNA_2G` set) | EFUSE word 0x1B |
| Firmware | **1st 4 KB half of `rt2870.bin`** (md5 `d94f0280cf9980999dbc2b999b281edb`) | wire FW multiwrite (frames 2041-2167) == `rt2870.bin[:4096]` |

> **Correction (2026-06-09 port):** the firmware is the **first** 4 KB of `rt2870.bin`, **not** the second. The kernel `rt2800usb_write_firmware` selects `offset=0` for RT2860/RT2872/**RT3070** (`offset=4096` is for everyone *else* — and that 2nd half *is* `rt5572.bin`, md5 `8d98ca9f…`, a different card). The wire confirms: the 64×64-byte multiwrite to `0x3000` byte-matches `rt2870.bin[:4096]`. Shipped as `assets/rt3070_fw.bin`.

**Not the counterfeit case.** Unlike the blank-EFUSE RT3572 (`NIC_CONF0=0x0000`, RF/cal all
`0xFF`, degenerate rx-filter cal), this card's EFUSE is properly factory-burned. You will get
real values.

**The driver reads + parses the EFUSE at runtime and branches on whatever it finds — exactly like
the kernel; nothing about this card is hardcoded.** `eeprom.read_eeprom_efuse` reads the chip's EFUSE
over the transport; `parse_eeprom` decodes the bytes; every downstream write keys off the parsed
fields (RF type, chains, freq_offset, txmixer-gain, ext-LNA, LED) and the MAC_CSR0 chip rev. The
table above is a **decoder-validation reference** (what *this* card's EFUSE holds, to confirm the
parser) — not values baked into the code. Porting needed **zero live hardware**: the EFUSE reads are
in the capture, the gate replays them, so the decoder is validated entirely offline
[[feedback_porting_no_hardware]].

**Completeness gap — CLOSED.** `rt2800_validate_eeprom` (rt2800lib.c:11026) is ported in
`eeprom.validate_eeprom` and wired into bring-up between the EFUSE read and `parse_eeprom`
(both the gate's `_walk_init` and `driver._bringup`). It fills defaults for blank/invalid fields
on the in-RAM image only (no chip write); a no-op on this factory-burned EFUSE (`#TODO untestable`
— every guard is false here), exercised only by a blank/counterfeit EFUSE. Gate still PASSES.

---

## Port status — COMPLETE (autonomous session 2026-06-09)

**The entire capture is byte-perfect — `verify_pcap.py rt3070` PASSES.** It reproduces **all
8879 control ops of capture-1** with a single cursor — cold bring-up (probe → EFUSE → firmware
→ MAC/BBP/RFCSR init → radio-on) *and* the full operational phase (airmon monitor entry, every
airodump/iw channel hop) — waiving only the **678 `TX_STA_FIFO` (0x1718) polls** aireplay fires
during its human-fired `--test`/`-0` deauth (bulk-OUT TX is out of the control gate by design).

| Done | What | Wire span | Module(s) |
|------|------|-----------|-----------|
| M1 | probe_rt + EFUSE (word-offset) + firmware load + MCU boot | op0–310 | transport, eeprom, firmware, mac |
| M2d | enable-radio prologue + MAC `init_registers` (256× WCID/IVEIV/beacon clears, config_filter) | op311–1203 | mac |
| M2e | BBP/RF-ready wait + boot signal + `init_bbp` (30xx + EEPROM overlay) | op1204–1248 | bbp, mac |
| M2f | `init_rfcsr_30xx` + RX-filter calibration (returns `calibration_bw20` via `state.DrvData`) | op1249–1403 | rfcsr, state |
| M3 | enable_radio tail: MCU_CURRENT, RX enable, LED config + start_queue | op1404–1426 | mac |
| M4 | channel tune: `config_channel` → `config_channel_rf3xxx` (RF3020) + EEPROM txpower decode | op1474–1572 (per hop) | chan |
| M-op | monitor entry: `config_filter` 0x97→0x93 + post-radio config + `rt2x00mac_config` hop wrapper | op1427–1468 | monitor, mac |
| M5 | link tuner: `reset_tuner` BBP66 VGC (per-config) + periodic `link_tuner` (#TODO STA-only) | per hop | link_tuner |
| M6 | RX decode (RXINFO/RXWI/RXD) + TX wiring (wire-only) + `RT3070Driver` + `validate_eeprom` | — | rx, tx, driver, eeprom |

Rev gating verified for **REV_RT3070F** (the `LDO_CFG0` write, RT3071/3090 branch, trailing
`RFCSR27=0x03` all correctly skipped, ported `#TODO untestable`). EFUSE **word-offset** reader
confirmed. RX-filter calibration's BBP55 feedback loops reproduce off the replayed reads.

### Operational-phase decode (now reproduced, not waived)

The monitor entry is `rt2x00mac_configure_filter` (interface-up `0x97`, then monitor `0x93` once
`CONFIG_MONITORING` is set — both *driven*, both flag-derived) bracketing the post-radio
`rt2x00mac_config` (POWER|RETRY|PS: `config_txpower` + `config_retry_limit` + `config_ps` +
`config_ant` + `reset_tuner`). Each airodump/iw hop is a full `rt2x00mac_config(CHANGE_CHANNEL)`:
**stop RX → update_survey → config_channel → config_txpower → reset_tuner → config_ant →
reset_tuner → start RX** (one stop / one start — the antenna reconfigure's refcounted
`start_queue` is the lone RX re-enable). The ~120-op block repeats per channel; the gate peeks
the channel from the wire (rf1+rf3) and dispatches `chan.set_channel`.

**The link tuner emits nothing in this capture:** it is gated on `intf_sta_count` (rt2x00link.c),
which a monitor-only interface never increments, so the periodic ~1 Hz worker never schedules.
The BBP66 AGC writes seen on the wire are `reset_tuner` (default VGC), run on every channel config.
`config_erp` is likewise BSS-change driven (no BSS in monitor) — ported for a future STA port,
not called here. **M4/M-op verification target met:** ch1 tune byte-matches (`rfcsr12=0x66`
= `default_power1[ch1]=0x06` decoded from EEPROM TXPOWER_BG1, `rfcsr23=0x0a`=freq_offset,
`rfcsr24/31=0x08`=calibration_bw20); TX_PWR_CFG_0..4 reproduce the byrate table with delta=0.

### Open items / for the human

- **Hardware RX benefit-check — DONE, kernel-parity.** `beacon_watch.py` live (ch1, 12 s) on the
  real card brought up cleanly (no wedge), tuned, and received **8.4 beacons/s** for the best AP
  (~86 % of the 9.77/s single-AP ceiling, **zero stall-seconds**, ~1000 beacons across all APs).
  The `beacon_watch_usbcap.py` baseline (the kernel driver's fixed-ch1 segment of capture-1) is
  **8.9/s / 91 %** for the same AP — i.e. our userland driver matches the kernel within run
  variance. RX path confirmed on hardware. (airodump's `.cap` is useless as a baseline — it
  dedups to 1 beacon/AP; use `beacon_watch_usbcap` on the usbmon pcap.)
- **Live TX** (inject/deauth) — the human's gate [[passive_by_default]]. `tx.py` builds the
  descriptor (QSEL=2, +4 USB end pad, TX_OP=HT_NONE, WIV=1) but never fires.
- **capture-2 / capture-3 init divergence at op311** — a *pre-existing* M1 quirk: those cold boots
  emit the MCU boot signal (`HOST_CMD=0x72`) **twice** (op310 *and* op311); capture-1 emits it once.
  A non-deterministic firmware-boot retry, untouched by the operational work. Reproducing the
  variable boot-signal count is a separate M1 investigation.

### Operational phase — pattern + decoded map (for the next session)

Follow the **rtl8188eus_dkms shape** (`chips/rtl8188eus_dkms/{driver,monitor}.py` + its `verify_pcap.py`):
`connect()` runs the verified init then the monitor bring-up as discrete handlers in wire order
(`enable_rx_bar` → `set_channel(ch1)` → `enter_monitor`), starts the RX reader + the periodic
watchdog; the gate's `_walk_operational` dispatches the **same** handlers at unique wire **openers**,
peeking the channel from the wire (`_peek_channel`), reproducing everything and waiving only the
external tool (aireplay). For rt3070 the analog is `chan.set_channel` (config_channel) + a
`monitor.py` `enable_monitor` (config_filter → `0x93`).

**rt2800 vs rtl8188eus:** rtl8188eus's monitor entry writes fixed RXFLTMAP/RCR values; rt2800's
`config_filter` is a flag-driven RMW, so the wire shows the interface-up filter (`RX_FILTER_CFG`
`0x97`, `DROP_NOT_TO_ME` still set) **then** the monitor filter (`0x93`, `CONFIG_MONITORING` set).
**Reproduce both** by driving the monitor-entry call sequence faithfully — `config_filter` with the
interface-up flags, then with monitor flags — exactly as `rt2x00mac_configure_filter` did. Both
values are a deterministic function of the flags + the `CONFIG_MONITORING` state, which we control;
nothing here is waived.

**Decoded op1427+ map (capture-1):** the monitor entry is several mac80211 callbacks interleaved —
`configure_filter`(0x97) → `rt2x00mac_config`{ stop_queue(RX): `MAC_SYS_CTRL`=0x4 → `rt2800_config`
(config_lna_gain + **config_channel** + **config_txpower**) + `config_ant` (BBP1/BBP3) → start_queue(RX):
`MAC_SYS_CTRL`=0xc } → `configure_filter`(0x93 monitor) → … → the RFCSR channel tune (`0x0500`) at op1474.

**M4 needs first:** the EEPROM **TX-power decode** (`rt2800_config_txpower` / `rt2800_txpower_to_dev`
@ rt2800lib.c:4112 + the per-channel `EEPROM_TXPOWER_BG*` tables) to fill `channel_info.default_power1/2`,
which `config_channel_rf3xxx` consumes. Then thread `calibration_bw20` (from M2f's `rx_filter_calibration`)
+ `freq_offset` (10) into `config_channel_rf3xxx`.

**RF3020 2.4 GHz channel table** (`rf_vals_3x`, rt2800lib.c:11435 — `{ch, rf1, rf2, rf3}`; this card is
2.4 GHz-only, RF3020, so chs 1–14 only):
`1:{241,2,2} 2:{241,2,7} 3:{242,2,2} 4:{242,2,7} 5:{243,2,2} 6:{243,2,7} 7:{244,2,2} 8:{244,2,7}`
`9:{245,2,2} 10:{245,2,7} 11:{246,2,2} 12:{246,2,7} 13:{247,2,2} 14:{248,2,4}`

**M4 verification target — the default channel-1 tune** (capture-1 op1474–1527, `config_channel_rf3xxx`):
`rfcsr2=0xf1`(rf1=241,ch1), `rfcsr3.K=2`, `rfcsr6.R1=2`, `rfcsr12=0x66`(default_power1),
`rfcsr13=0x00`(default_power2), `rfcsr1=0xf1`(1T1R RX/TX PD), `rfcsr23=0x0a`(freq_offset 10),
`rfcsr24=0x08`+`rfcsr31=0x08`(calibration_bw20 — the return of M2f `init_rx_filter(bw20)`; thread it
out of `rx_filter_calibration`), `rfcsr7|=RF_TUNING`, `rfcsr30` RF_CALIBRATION pulse. The
`default_power1=0x66 / default_power2=0x00` for ch1 is exactly what the EEPROM TX-power decode must
reproduce — verify the txpower port against it. `config_channel_rf3xxx` is gate-reachable once the
op1427–1473 monitor-entry prefix is dispatched, OR verify it standalone by replaying op1474+ against
`set_channel(ch1)` (targeted replay — sidesteps the prefix).

---

## The shared EFUSE bug (so you write yours correctly)

Kernel `rt2800_read_eeprom_efuse` (`rt2800lib.c:10955`):
```c
for (i = 0; i < EEPROM_SIZE / sizeof(u16); i += 8)   /* i is a u16 WORD index: 0,8,16,... */
    rt2800_efuse_read(rt2x00dev, i);                 /* EFUSE_CTRL_ADDRESS_IN = i (words) */
```
`EFUSE_CTRL_ADDRESS_IN = FIELD32(0x03fe0000)` (`rt2800.h:656`). Each iteration reads 16 bytes
(`EFUSE_DATA3..DATA0`, "read from end to start", each `cpu_to_le32` into `eeprom[i..i+6]`).

The shared `chips/rt2800usb/eeprom.py` loops `range(0, 512, 16)` and sets `ADDRESS_IN = byte
offset` → from block 1 on it fetches from **double** the address. Block 0 (MAC) is correct,
which is why nobody noticed. **Your reader must pass the WORD offset.** The gate catches the
wrong one on the 2nd block (verified).

EEPROM word map (`rt2800lib.c:308-347` `rt2800_eeprom_map`): MAC `0x02–0x04`, `NIC_CONF0
0x1A`, `NIC_CONF1 0x1B`, `FREQ 0x1D`, `LNA 0x22`, `RSSI_BG 0x23`.

---

## The gate

```
uv run python scripts/verify_pcap.py rt3070            # capture-1 (default cold-boot)
uv run python scripts/verify_pcap.py rt3070 capture-2  # or -3
```
`scripts/rt3070/verify_pcap.py` is already written — a single-cursor, fail-closed walk
modelled on the gold standard `scripts/rtl8188eus_dkms/verify_pcap.py`. It drives YOUR real
`chips/rt3070` handlers against a `ReplayDevice` that replays the chip's recorded
`ctrl_transfer`s, so every helper (regbusy poll, `write_multi` chunking, the EFUSE walker)
replays unchanged. It runs today and reports `FRONTIER: op 0` until you scaffold the driver.

`_walk_init` lists the milestones in source order — implement each, re-run, advance the
frontier. `_walk_operational` is a TODO template: dispatch the airodump/iw channel hops and
the link tuner to your real handlers (carry channel + AGC state across fires) once you reach
the operational frontier. Wire-format note: aireplay's injected frames are **bulk-OUT**, so
they never enter the control op stream — no waiver needed for them.

---

## Source map — bring-up in order (cite `[SRC]` from these)

All in `data_dumps/rt2x00-source-v6.18/`. The USB layer (`rt2x00usb.c`) gives the transport;
`rt2800usb.c` the USB-specific glue; `rt2800lib.c` the chip logic.

| M | What | Kernel function(s) |
|---|------|--------------------|
| M1 | FW upload + MCU boot | `rt2800usb.c` `rt2800usb_write_firmware` (~205-265); MCU boot via `rt2800_mcu_request` |
| M2b | USB bootstrap (`USB_DEVICE_MODE`, resets) | `rt2800usb.c` `rt2800usb_init_registers` |
| M2c | EFUSE detect + dump (**word offset**) | `rt2800lib.c` `rt2800_efuse_detect` (10894), `rt2800_efuse_read` (10909), `rt2800_read_eeprom_efuse` (10955) |
| M2d | MAC config block | `rt2800lib.c` `rt2800_init_registers` |
| M2e | BBP init (30xx) | `rt2800lib.c` `rt2800_init_bbp` dispatch (7255-7266 → `RT3070`) → `rt2800_init_bbp_30xx` (6521) |
| M2f | RFCSR init (30xx) + rx-filter cal | `rt2800lib.c` `rt2800_init_rfcsr` dispatch (10748 → `RT3070`) → `rt2800_init_rfcsr_30xx` (7618-7686); `rt2800_rf_init_calibration` (7625); `rt2800_rx_filter_calibration` (called 7677); `rt2800_normal_mode_setup_3xxx` (7685) |
| M3 | enable radio + RX filter (monitor) | `rt2800lib.c` `rt2800_enable_radio`, `rt2800_config_filter` (monitor: clear `DROP_NOT_TO_ME`) |
| M4 | channel tune (RF3020) | `rt2800lib.c` `rt2800_config_channel` dispatch (4187 `case RF3020` → `rt2800_config_channel_rf3xxx`, 2547); channel tables (RF3020: 11208 / 11807 / 11962) |
| M5 | link tuner (~1 Hz BBP66 AGC) | `rt2x00link.c` (the periodic AGC work — an async operational producer) |
| M6 | TX desc (WIRE only; human fires) | `rt2800usb.c` `rt2800usb_get_tx_data_len` (440-451), `rt2800usb_write_tx_desc` (411-427); `rt2800lib.c` TXWI builder |

**RT3070F-specific (this card's rev 0x0201):** in `init_rfcsr_30xx`, `rt2x00_rt_rev_lt(dev,
RT3070, REV_RT3070F)` is **false**, so the `LDO_CFG0` BGSEL/VLEVEL write (7648-7651) and the
trailing `RFCSR27=0x03` (7679-7682) are **skipped** on this card; the `RT3071/RT3090` branch
(7652-7675: RFCSR31, RFCSR6.R2, LDO_CFG0 DAC_TEST, GPIO_SWITCH) is **not taken**. Port all of
it faithfully (so the rt-checks are correct), but only the `else`-less common table + cal run
here — the wire will confirm.

---

## Multi-antenna / chain handling (the lead's "support every card" rule)

This card is 1T1R (RF3020) — the only chain config we can hardware-test. But `init_rfcsr_30xx`
/ `init_bbp_30xx` / `config_channel_rf3xxx` are shared with RT3071/3072/3090 (RF3021/3022,
2T2R). Chain count comes from EFUSE `NIC_CONF0` (txpath/rxpath), queried per device. **Port
the multi-chain writes** (2nd PA enable, 2nd-chain RFCSR/BBP, `disable_unused_dac_adc` gating)
faithfully from source, gated on the real chain count / rt-version — then mark the branches we
can't reach `#TODO untestable: no RT3071/3072/3090 hardware`. Do not hardcode `chains=1`.

---

## Captures + frame ranges (`usb_dumps_new/captures_rt3070/`, capture-1)

| Phase | Frames | Maps to |
|-------|--------|---------|
| plug-in → probe bring-up | 482–1942 | connect() cold bring-up (M1–M4) |
| `airmon-ng start` (monitor entry) | 1943–6184 | `config_filter` monitor + radio enable (M3) |
| airodump `--band abg` 250 ms hop | 6185–13317 | channel hopper → `set_channel` (M4, operational) |
| `iw set channel 1..12,1` | 19703–27714 | per-hop `set_channel` |
| `aireplay --test` + `-0 1` deauth | 27715–32732 | **TX (bulk-OUT, out of scope for the ctrl gate; human fires live)** |

capture-2 / capture-3 are repeat cold-boots — use them as cross-checks. Card is 2.4 GHz-only
(main.log: "5GHz Support NOT detected"), so SUPPORTED_CHANNELS = 1..14, 20 MHz width.

---

## Module layout to build (`chips/rt3070/`)

Mirror the per-chip shape (`CLAUDE.md` → "Adding a New Chipset"); the gate's `_walk_init`
already expects these names (adjust both together if you rename):

```
transport.py  RT3070Transport  — ctrl_transfer wrapper: read32/write32, write_multi/read_multi
                                  (chunk to 64B per [[rt2x00usb_csr_cache_size]]), single_write,
                                  device_mode.  [SRC] rt2x00usb.c
firmware.py   load_firmware_blob(), upload(t, blob)            [SRC] rt2800usb.c write_firmware
eeprom.py     read_eeprom_efuse(t)  ← WORD OFFSET, parse_eeprom(buf)->EepromValues
mac.py        read_chip_id, read_perm_mac, is_chip_warm, usb_init_registers, init_registers,
              enable_radio, config_filter (monitor), write_mac_address
bbp.py        prepare_bbp(t), init_bbp_30xx(t, ev)
rfcsr.py      init_rfcsr_30xx(t, ev)  (incl. rf_init_calibration + rx_filter_calibration)
chan.py       set_channel(t, ev, ch)  → config_channel_rf3xxx (RF3020)
link_tuner.py the ~1 Hz BBP66 AGC tuner                        [SRC] rt2x00link.c
rx.py         parse_rx_urb, probe_endpoints, read_rx_burst     [SRC] rt2800usb.c RX desc (481-518)
tx.py         build TXINFO/TXWI + bulk-OUT (wire only)         [SRC] rt2800usb.c (411-451)
driver.py     RT3070Driver (WlanDriver Protocol); SUPPORTED_IDS = [148f:3070];
              SUPPORTED_CHANNELS = list(range(1,14)); register in wlan/manager.py _all_drivers()
constants.py  register addrs + BIT(n) — grep verbatim from rt2800.h, never typed from memory
assets/rt3070_fw.bin  — extract from the pcap (writes to FIRMWARE_IMAGE_BASE 0x3000), byte-verify
                        == the 2nd half of linux-firmware rt2870.bin (md5 above)
```

Family wire-format **scars** worth honoring (read `chips/rt2800usb/RT2800USB.md` §RX/TX desc
+ §EFUSE, but **re-derive the values from source and confirm on THIS wire** — the EFUSE bug
proves our code can be wrong): RX L2-pad removal before FCS trim; RX frames are FCS-stripped
[[project_rx_frames_include_fcs]]; TX needs the +4 USB end-pad, `QSEL=2`, bulk-OUT EP `0x02`,
and `SHARED_KEY_MODE` cleared so injects aren't WEP-encrypted with a zero IV.

---

## Hardware loop (agent-driven; card is plugged + Zadig-bound)

Per [[feedback_collaboration]] / `planning/PORTING.md`: you propose milestones, pcap-verify
each, run a `scripts/rt3070/test_hw.py` (write one, model on
`scripts/rt2800usb/test_hw_rt2800usb.py`) on the milestones where it makes sense, commit each
milestone separately. RX-health A/B via `beacon_watch.py` (live) vs `beacon_watch_usbcap.py`
(this capture's beacon count). If the device wedges, ask the user to replug (resets cold-boot
state; Windows+WinUSB can lose the binding). **The only human gate is live TX.**

## Port-completeness audit (before declaring done — `verify_pcap` green ≠ faithful)

Green gate + healthy beacons is necessary, not sufficient (hardcoded-to-the-wire constants
can't be validated by that wire). Do the comment-blind audit from `planning/PORTING.md`
§"Green ≠ faithful": walk the kernel call graph, classify each leaf
`faithful / hardcoded / omitted / N-A`, cite each "faithful", and prioritise conditional /
per-card / runtime leaves (efuse fields, chain count, freq/LNA/RSSI, link tuner, rx-filter
cal). Record verified facts in this doc as you go; default-assume every `always/never/skip`
comment is wrong until source or silicon proves it.
